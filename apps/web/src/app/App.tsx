// Placeholder shell — auth screens and the app router land with the
// frontend portion of the build (see docs/ARCHITECTURE.md milestones).
export function App() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Task Tracker</h1>
      <p>API foundation is live — frontend screens are next.</p>
    </main>
  );
}
